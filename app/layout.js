import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import Navbar from "@/components/Navbar";
import Footer from "@/components/Footer";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "Finance Trakcer",
  description: "Expends Tracker is a full-stack expense management web application built with Next.js, React, shadcn/ui, Recharts, and MongoDB. It allows users to track their daily transactions, analyze category-wise spending, set monthly budgets, and view insightful summaries — all in a clean and responsive dashboard.",
  icons: {
    icon: '/favicon.png',
  }
};


export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <Navbar />
        {children}
        <Footer />
      </body>
    </html>
  );
}
